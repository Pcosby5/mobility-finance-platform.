[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [".local", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.lint.per-file-ignores]
# Django generates long help-text strings in migrations.
"**/migrations/*.py" = ["E501"]
